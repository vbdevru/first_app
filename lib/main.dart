import 'package:flutter/material.dart';

void main() {
  runApp(
    const Center(
      child: Text(
        'Привет флатерщик!',
        textDirection: TextDirection.ltr,
        style: TextStyle(fontSize: 70),
      ),
    ),
  );
}
